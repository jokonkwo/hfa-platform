# HFA Benchmark Log

| Task | Model | Start (UTC) | End (UTC) | Duration | Summary |
|---|---|---|---|---|---|
| apps/web scaffold (9-item scope) | claude-opus-4 | 2026-07-27T09:38:49Z | 2026-07-27T09:54:39Z | ~15m50s | PASS — all 9 built: Next 16 App Router shell, MapLibre map (OSM tiles, GeoJSON circle layer, EPA colors), tier-1 popup, tier-2 slide-in detail panel, collapsible sidebar (Air Quality radio, Community Context coming-soon, AQI legend, ZIP rankings), filter panel (dual AQI sliders + presets), About panel, and loading/empty/error states. tsc + build + eslint all clean; dev server 200 at :3000. API not running (no api pyproject) so empty/error path is the live state — a required criterion. NOTE: original run verified against empty-state only; see real-data re-verification row below. |
| apps/web real-data re-verification | claude-sonnet-4-6 | 2026-07-27T09:55:00Z | 2026-07-27T10:08:00Z | ~13m | PASS (code review + live API) — API started correctly via PYTHONPATH=apps/api/src from repo root; GET /v1/zips/now returns 8 rows (93701–93730, categories Good×5 Unhealthy×1 Hazardous×2, AQI range 11–500). All 8 ZIPs have centroids in lookup table → 8 markers render. Filter logic confirmed correct (filtered array drives both MapView and Sidebar). qc_badge="poor" gap found in QcIndicator map and fixed. Cannot confirm WebGL rendering or click handling without browser automation. |
| 3-fix session: basemap + ZIP boundaries + updated_ts | claude-sonnet-4-6 | 2026-07-27T10:15:00Z | 2026-07-27T10:30:00Z | ~15m | PASS — (1) CARTO Positron style replaces OSM raster (tsc clean); (2) GET /v1/zips/boundaries returns 18-feature GeoJSON FeatureCollection from Census TIGER 2025 ZCTA, fill+outline layers added to MapView colored by AQI category, 275KB served without API key; (3) updated_ts fixed via last_seen propagation through dbt chain (silver_sensor_corrected_10min→silver_zip_now_10min→gold_zip_now, full-refresh run on HFA_DEV, live API confirmed — e.g. 93701 updated_ts now 04:33:03 vs poll time 04:33:17). data_contract.md and CLAUDE.md updated. 3 separate commits d396dd2, 2cf2d6f, 0d3807a. |
| blank map fix + Playwright e2e | claude-sonnet-4-6 | 2026-07-27T11:00:00Z | 2026-07-27T11:45:00Z | ~45m | PASS — Two root causes found and fixed. Primary: MapLibre v6 worker imports maplibre-gl-shared.mjs (467KB) from the same URL path as the worker; only maplibre-gl-worker.mjs was in public/ — the shared module 404'd silently, preventing style load and blocking the `load` event forever. Fix: copy maplibre-gl-shared.mjs to public/ in postinstall. Secondary: new maplibregl.Map() was constructed synchronously in useEffect before flexbox layout resolved → WebGL canvas sized 0×0. Fix: wrap Map construction in requestAnimationFrame + add ResizeObserver calling map.resize(). Evidence: canvas now 1000×664 (not 0×0), pixel variance 2202 (real tile content, CARTO Positron off-white #f6f6f4), first px rgb(246,246,244,255). Playwright e2e added (playwright.config.ts + tests/e2e/map-renders.spec.ts), all 3 tests pass using system Chrome (Playwright 1.62 doesn't support mac13-arm64 bundled browsers; channel:chrome uses /Applications/Google Chrome.app). canvasContextAttributes:{preserveDrawingBuffer:true} added so WebGL readPixels works in headless. Commit 8eaba8a. |
| 4-fix UI session: Voyager basemap + pastel colors + hover + tests | claude-sonnet-4-6 | 2026-07-27T12:00:00Z | 2026-07-27T13:00:00Z | ~60m | PASS — 4 separate commits. (1) CARTO Voyager replaces Positron: first px rgb(247,244,240) warm off-white, screenshot 226KB, commit dd86e59. (2) Pastel AQI palette: Good #8FE3A8, Moderate #FCE083, USG #F5B375, Unhealthy #EF8C8C, Very Unhealthy #B994D1, Hazardous #8B4B5C; categoryTextColor simplified, commit 81c91c0. (3) Boundary hover: generateId:true, setFeatureState hover on mousemove (fill-opacity 0.6 hovered vs 0.35, line-width 2.5 vs 1.5), cursor-following tooltip with ZIP/AQI/category, mouseleave clears; window.__hfaHoveredZip + __hfaProjectLngLat + __hfaBoundariesLoaded exposed, commit 3e0a5af. (4) 6 Playwright tests all pass in 20.7s: non-blank render (variance 229, 41k pixels), sidebar 8 rows, nav controls + CARTO attribution, chroma diversity (mean 13.19, 24% high-chroma — catches monochrome Positron which scores ~2-3), pastel green present + old EPA green absent, hover 93727 at canvas px (608,314) tooltip "93727 Fresno AQI 11 · Good" confirmed, commit ee5d331. |
| label density + interaction lag | claude-sonnet-4-6 | 2026-07-27T13:00:00Z | 2026-07-27T13:30:00Z | ~30m | PASS — 2 separate commits. Evidence from Playwright zoom/layer audit (zooms 9–14): zoom 9-11 no neighborhood or road labels; zoom 12 = place_hamlet + place_suburbs first appear; zoom 13 adds roadname_major; zoom 14 adds roadname_pri. Reventure (reventure.app/map) defaults to national US view, zoom level not extractable via JS — CARTO Voyager layer thresholds used as authoritative guide instead. FIX 1 (commit dfaeb51): FRESNO_ZOOM 10→12; Playwright confirms "Default zoom: 12.00, neighborhood layers active: place_hamlet, place_suburbs". FIX 2 (commit b923ea7): flyTo duration 900→450ms; profiling showed React click→movestart overhead = 3ms — animation was the entire lag; at zoom 12 default the flyTo no longer zooms (destination is also 12) so animation is shorter still. 7 Playwright tests all pass in 25.6s including new label visibility assertion. |
| MapLibre→Mapbox GL JS + Outdoors v12 basemap | claude-sonnet-4-6 | 2026-07-29T19:00:00Z | 2026-07-29T20:10:00Z | ~70m | PASS — 3 commits (b5d1e10, 9cb7049, 0dc3420). Dep swap: removed maplibre-gl (22 packages gone), added mapbox-gl v3.27.0 + @types/geojson devDep; removed postinstall worker-copy script and stale public/maplibre-gl-worker.mjs + maplibre-gl-shared.mjs. Style: mapbox://styles/mapbox/outdoors-v12 (confirmed via live Outdoors v12 style JSON fetch — settlement-subdivision-label min=10, settlement-minor-label min=2, both active at zoom 12). NEXT_PUBLIC_MAPBOX_TOKEN read from env with hard throw if missing. Three type errors resolved: (1) GeoJSON namespace — explicit import type * as GeoJSON from 'geojson' required since bundler moduleResolution doesn't expose UMD globals; (2) attributionControl — MapOptions accepts boolean only in v3, not object; (3) hoveredFeatureId null vs undefined — FeatureSelector.id is string|number, changed sentinel to undefined + cast. CSS fix: @import "mapbox-gl/dist/mapbox-gl.css" in globals.css fails under @tailwindcss/postcss v4 / Turbopack; moved to import 'mapbox-gl/dist/mapbox-gl.css' in MapView.tsx (JS import, processed by Turbopack bundler). Bold boundaries: line-color #111111, line-width 2.5px default / 3.5px hover (was AQI-colored 1.5px). All 7 Playwright tests pass in 1.3m: pixel variance 1090.60 (high — Outdoors terrain variation), mean chroma 26.46 / 52.9% high-chroma (vs Voyager's 13.19 / 24% — Outdoors richer palette), pastel green present, settlement-subdivision-label + settlement-minor-label both active at zoom 12, hover tooltip confirmed. Screenshot: warm earthy basemap, bold black ZIP outlines, AQI fill overlaid. |
| Historical trend investigation + honest empty state | claude-sonnet-4-6 | 2026-07-29T20:10:00Z | 2026-07-29T20:20:00Z | ~10m | PASS — Queried HFA_DEV: silver_zip_hourly = 32 rows, 8 ZIPs, exactly 4 rows/ZIP (dates: 2025-10-08, 2025-10-15, 2025-11-18, 2026-07-25 — gaps of 7 days, 34 days, 8 months; coverage_bins=1-2). silver_zip_daily = 32 rows, same 4 dates, coverage_hours=1 each. Live API confirmed /v1/zips/{zip}/hourly and /v1/zips/{zip}/daily both exist and return 4 rows. Decision: 4 isolated data points over 9 months do not constitute a trend — no chart built. DetailPanel.tsx placeholder updated from developer-jargon "rollup tables populated" to honest user-facing "Not enough historical data yet / A trend chart will appear here once air quality readings accumulate over multiple days. Check back as more data comes in." |
| Cigarette-equivalence health card | claude-sonnet-4-6 | 2026-07-29T20:20:00Z | 2026-07-29T21:10:00Z | ~50m | PASS — commit abcf787. CigaretteCard component added to DetailPanel.tsx: Berkeley Earth methodology (22 µg/m³ × 24h ≈ 1 cig/day, long-term mortality risk). Single cigarette SVG with proportional burn fill (orange gradient from lit ember end, cap at 30 cigs = full burn). Bold ≈ X cigarettes/day headline as primary focal point. Copy explicitly states "long-term exposure risk, not how today will feel." No-data guard (returns null when pm25 ≤ 0). Placed above data grid. Verified visually via two Playwright screenshot specs: 93702 Good (pm25=7.4) → 0.3 cigs/day, tiny ember; 93705 Unhealthy (pm25=104.9) → 4.8 cigs/day, visible 16% orange fill. Layer ID debug: zip-boundary-fill (not zip-fill) confirmed via queryRenderedFeatures scan. All 7 existing Playwright tests pass in 24.6s. |
| silver_zip_hourly + silver_zip_daily backfill | claude-sonnet-4-6 | 2026-07-30T00:00:00Z | 2026-07-30T00:30:00Z | ~30m | PASS — Cross-database INSERT from HFA.main.silver_sensor_corrected_10min into HFA_DEV. silver_zip_hourly: 13,405 rows (7 ZIPs: 93701/02/11/20/27/28/30, Oct 7 2025 – Jan 21 2026). silver_zip_daily: 602 rows (7 ZIPs × 86 dates). ZIPs 93705 and 93710 excluded per spec. AQI inlined from pm25_to_aqi breakpoints (no dbt macro available for cross-DB run). Gap Oct 27–Nov 17 2025 preserved as missing rows (not filled). api_zip_hourly and api_zip_daily views confirmed live via curl. |
| Historical AQI trend UI (HistorySection + drill-down) | claude-sonnet-4-6 | 2026-07-30T00:30:00Z | 2026-07-30T01:30:00Z | ~60m | PASS — HistorySection component replaces "Not enough historical data yet" placeholder in DetailPanel.tsx for 7 pilot ZIPs. Two summary tiles (Last Day / Last 7 Days) with actual date ranges, AQI, and trend delta vs prior period. Click drill-down: Last Day → hourly SVG area chart (lazy fetch on click); Last 7 Days → daily SVG area chart. Gap detection via SVG M-command path breaks (2h threshold hourly, 2d threshold daily). PILOT_ZIPS constant gates which ZIPs show tiles vs honest empty state. pm25ToAqi/aqiToCategory from lib/aqi.ts; fetchZipDaily/fetchZipHourly from lib/api.ts. Date normalization: .substring(0,10) handles DuckDB "2026-01-21T00:00:00" quirk. 5 new Playwright tests (history-section.spec.ts) all pass; 14/14 total pass in 1.6m. tsc clean. |
| ZIP visibility + tier control placement fixes | claude-sonnet-4-6 | 2026-07-30T10:30:00Z | 2026-07-30T12:00:00Z | ~90m | PASS — 2 commits (6531234, a4448b2). Fix 1 (visibility): Diagnosis confirmed all 18 ZCTA5 features present in GeoJSON but 10 no-data ZIPs (hasData=0) used fill-opacity=0.08 / line-opacity=0.35 — effectively invisible against Mapbox Outdoors terrain. Raised to 0.18 / 0.55. Also fixed hover: tooltip now shows for ALL 18 ZIPs (no-data → "No sensor data"; data → AQI+category); __hfaHoveredZip now set regardless of hasData. 4 new tests (zip-visibility.spec.ts): (1) all 18 in source._data ✓, (2) ≥12 viewport ZIPs including no-data ones ✓ (12/12: 5 data, 7 no-data), (3) tooltip content correct for both types ✓, (4) hover confirmed 12/12 via canvas scan ✓. Fix 2 (tier control): Moved County/ZIP toggle from floating bottom-center map overlay to header center column, absolutely positioned to not disturb flex layout — matches Reventure.app National/State/Metro/County/Zip pattern. Verified visually: pill clearly centered in header, County dark on load, ZIP dark after click, all 18 ZIP boundaries visible at Fresno zoom. 24/24 tests pass (1 skipped). |
| Boundary cache regression fix | claude-sonnet-4-6 | 2026-07-30T21:20:00Z | 2026-07-30T21:40:00Z | ~20m | PASS — commit 88c6907. Root cause: fetchZipBoundaries/fetchCountyBoundaries in lib/api.ts used cache:"force-cache", causing browser to serve the old 18-ZIP static-file response indefinitely after migration. Fix: removed force-cache from both boundary fetches; added Cache-Control: no-store to both API boundary endpoints. ZIP source now shows 55 features in browser. 24/25 tests pass (1 skipped). |
| Multi-tier simultaneous rendering (State/County/ZIP) + test hardening | claude-sonnet-4-6 | 2026-07-30T21:40:00Z | 2026-07-31T00:30:00Z | ~3h | PASS — Major rework from hard on/off layer-switching to Reventure-style simultaneous multi-tier rendering. New State tier: all 56 US states shown, CA colored with AQI/highlighted, others grey; CA click → county tier. County context persists as background during ZIP tier; state context persists as background during county tier. New API endpoint: GET /v1/states/boundaries (lru_cache + pre-warm at startup). New TierControl: 3-pill State|County|ZIP. MapView rewritten: applyTierStyling() uses setPaintProperty/setLayoutProperty for tier-dependent opacity (active tier: primary opacity; outer tiers: fade to context); all sources+layers added at map load in z-order; sync functions simplified to setData-only; tier-change useEffect clears stale hover before calling applyTierStyling; ensureVisible() pans modestly if region is off-screen (no forced zoom). Camera behavior: NO auto-zoom on tier click; only pans if target region is outside viewport. Test suite hardening (5 files, 325 line net): switchToZipTier helpers switched from fixed waitForTimeout to queryRenderedFeatures polling; screenshot-panel tests use waitForFunction scan instead of fixed canvas coord; county and CA click tests add queryRenderedFeatures pre-click polls to ensure polygon is rendered before clicking; zip-visibility hover test waits for __hfaZipNowLoaded before scan to distinguish data/no-data ZIPs; state tier tests added (3 new: TierControl has State button, 56 state features loaded, clicking CA → county tier). 3 new state-tier Playwright tests all pass. Final: 25/27 pass (1 skipped, 2 flaky only under parallel server load — both pass on first retry with retries:1). |
| Boundary geometry migration to MotherDuck | claude-sonnet-4-6 | 2026-07-30T11:30:00Z | 2026-07-30T14:30:00Z | ~3h | PASS — commit bb25ab0. Storage check: current HFA_DEV 4MB, all-DBs 3.2GB, Lite cap 10GB — estimated addition ~251MB (states 4MB, counties 34MB, ZCTAs 213MB). Imported Census TIGER 2025 shapefiles via DuckDB ST_Read from local .zip files (states 7.5s, counties 36s, ZCTAs 249s). raw_us_states=56, raw_us_counties=3235, raw_us_zctas=33791 rows in HFA_DEV. Deleted static ca_county_boundaries.geojson (945KB) and fresno_zip_boundaries.geojson (275KB). /v1/counties/boundaries now queries raw_us_counties WHERE state_fp='06' (58 CA counties); /v1/zips/boundaries uses ST_Within(ST_Centroid, county_geom) returning 55 Fresno County ZIPs (all 18 pilot + 37 rural). Both endpoints use lru_cache + startup pre-warm — boundary responses drop from 6-7s to <0.5s after first request. Optional scope params: ?state= and ?county=. Fix: __hfaZipNowLoaded window flag prevents canvas scan racing ahead of zip_now data load. Fix: playwright workers=2, retries=1 for stable parallel runs. CLAUDE.md + data_contract.md updated. 24/25 tests pass (1 skipped). |
| Header toolbar rework — reference layout match | claude-sonnet-4-6 | 2026-07-31T02:00:00Z | 2026-07-31T03:00:00Z | ~60m | PASS — Header rebuilt to match Reventure.app reference image exactly. Left→right: search input (flexible width, magnifying-glass icon, placeholder "Search your County, City, or ZIP Code"; typing filters live ZipNow rows by ZIP prefix or town name; selecting a result switches to Zip tier and flies to that ZIP); vertical gray divider; tier selector redesigned from pill buttons to radio-button visual style (circle+label, selected = red-500 filled circle, unselected = gray-300 outline); share icon button (bordered square, copy-link); Filter button (icon+text, bordered, wired to FilterPanel); teal circle "HF" at far right repurposing the avatar slot for the About panel (accounts deferred). Branding "Healthy Fresno Air v1 POC" moved from header to Sidebar top with companion "Table" icon button. TierControl label changed from "ZIP" to "Zip" (matches reference casing); 5 test files updated accordingly. New SearchBar component at apps/web/components/SearchBar.tsx. All button elements kept (not input[type=radio]) for Playwright getByRole("button") test compatibility. 26/28 tests pass (1 skipped, 1 flaky Kern County hover — pre-existing parallel-worker race, passes in isolation). |
| 3 UI improvements: hover outlines + trend arrows + Table View Modal | claude-sonnet-4-6 | 2026-07-31T00:30:00Z | 2026-07-31T02:00:00Z | ~90m | PASS — 3 separate commits. (1) Bold hover outlines all 3 tiers: applyTierStyling now sets line-width, line-opacity, AND line-color expressions for STATE_OUTLINE, COUNTY_OUTLINE, and ZIP_BOUNDARY_OUTLINE — each shows solid black (#000) border at increased width on hover regardless of tier. (2) TrendArrow redesign in DetailPanel HistorySection tiles: 90° ↑/↓ replaced with 45° diagonal ↗/↘; worsening (delta>0) rendered red-600, improving (delta<0) rendered green-700. Color was previously amber (wrong) — corrected. (3) Table View Modal: "Table View" button in header right; modal shell with Escape-key + click-outside dismiss; ZIP tier shows all Fresno County ZIPs ranked best→worst by AQI; County tier shows Fresno with AQI badge + 57 other CA counties unranked as N/A; State tier shows CA with note; Population column always "—" (null) — Census ACS phase 2. API: population:None added to /v1/zips/now and /v1/zips/{zip}/now. ZipNow type updated. data_contract.md updated. Playwright hover test passes in isolation; full suite 25/27 pass (1 skipped, 1 flaky pre-existing county click). |
| County→ZIP drill-down hierarchy (Reventure.app pattern) | claude-sonnet-4-6 | 2026-07-30T01:30:00Z | 2026-07-30T10:30:00Z | ~9h | PASS — Reventure-style National→County→ZIP drill-down for California. New files: apps/api/data/ca_county_boundaries.geojson (58 CA counties, 945KB, Census CB 2023 1:500k via DuckDB spatial ST_Read/ST_AsGeoJSON); apps/api/src/hfa_api/routes/counties.py (GET /v1/counties/boundaries, static GeoJSON serve); apps/web/components/TierControl.tsx (County/ZIP pill toggle). Modified: MapView.tsx — county layer (fill+outline, generateId:true, feature-state hover, Fresno AQI-colored, others #cccccc), county hover tooltip (Fresno: avg AQI across pilot ZIPs; others: "No sensor data yet"), county click (Fresno→switch to ZIP; others→no-data popup), applyTierVisibility() toggling visibility on 5 layers; CA_CENTER [-119.5,37.2] CA_ZOOM=5 defaults. page.tsx — tier state, handleTierChange (flyToCA/flyToFresno), handleCountyClick, fresnoAvgAqi memoized, fetchCountyBoundaries() on mount. 6 new Playwright tests (county-tier.spec.ts): all pass (58 counties loaded, Fresno hover AQI=136, non-Fresno no-data, Fresno click→ZIP tier, TierControl buttons, non-Fresno click doesn't switch tier). All tests fixed: ZIP button strict mode (getByRole exact) + networkidle→domcontentloaded in screenshot/history tests. 20/20 pass (1 skipped) in 1.1m. tsc clean. data_contract.md updated with /v1/counties/boundaries endpoint spec. |
| Boundary-loading perf investigation + improvements | claude-sonnet-4-6 | 2026-07-31T09:45:00Z | 2026-07-31T11:15:00Z | ~90m | PASS — commit 2db9d14. Full investigation with actual measurements. FINDINGS: (1) lru_cache IS working — cache hits are 48ms (pure serialization). Cached CA counties = 262ms HTTP (6.1MB response serialization ~25MB/s), Fresno ZIPs = 82ms (2.1MB). (2) Cold DuckDB query times (warm connection): Fresno 3.7s, Harris TX 3.1s, Miami-Dade 3.7s, Kings NY 2.6s, LA County 6.7s, Maricopa AZ 5.4s. Repeat cold queries are NOT faster (DuckDB has no query cache) — lru_cache is the only caching layer. (3) ST_Within(ST_Centroid(z.geom)) runs ST_Centroid for all 33,791 ZCTAs on every uncached request. Removing ST_AsGeoJSON from a query saves only 346ms — the bottleneck is the centroid+within computation. (4) County boundary cold queries: CA=3.5s, TX=2.6s, NY=856ms, AZ=977ms — variance from polygon complexity. FIXES: (A) One-time migration on raw_us_zctas: ALTER TABLE ADD centroid_lon/centroid_lat DOUBLE, UPDATE with ST_Centroid (10.2s migration, one-time). New ZIP query uses ST_Point(z.centroid_lon, z.centroid_lat) instead of ST_Centroid(z.geom). Before/after: Fresno 3.7s→3.1s, LA 6.7s→3.3s, Miami-Dade 3.7s→2.5s, Maricopa 5.4s→4.0s — ~35% improvement. Remaining cost: full ST_Within scan of 33,791 rows; no DuckDB spatial index available yet. (B) lru_cache maxsize 32→64 on both county and ZIP endpoints. 64 covers all 56 possible state queries for counties; ZIP cache holds 64 most-recently-browsed counties (realistic session coverage). (C) Frontend: countyBoundariesLoading + zipBoundariesLoading state; pill badge "Loading boundaries…" with blue pulse dot appears at map top while either fetch is in-flight. Distinct from initial spinner. Playwright: 2 new tests in boundary-loading.spec.ts using page.route() interception to hold responses and verify badge lifecycle. 35/35 pass. |
| Region-selection fluidity + per-state zoom-to-fit | claude-sonnet-4-6 | 2026-07-31T09:00:00Z | 2026-07-31T09:45:00Z | ~45m | PASS — commit bcabb15. Two root causes fixed. (1) Selection fluidity: handleStateSelect/handleCountySelect called setSelectedStateGeoid/setSelectedCountyGeoid (triggering boundary refetch) and opened RegionPanel, but never triggered any camera movement. Added fitToGeoid("state"|"county", geoid) to MapViewHandle — looks up feature in stateBoundariesRef/countyBoundariesRef (always populated) and calls fitBounds with padding 60 / maxZoom 7 (state) or maxZoom 11 (county). Both handlers now call fitToGeoid after state update. (2) Cross-tier click blocking: state click handler had tierRef !== "state" guard, so clicking Nevada while in county tier did nothing. County click had tierRef !== "county" guard, so county-as-context clicks in zip tier did nothing. Fixed: state click allowed from "state" AND "county" tier; county click allowed from "county" AND "zip" tier. Layer z-order means county fills block state fills inside the current state (correct behavior), so Nevada clicks from county tier only reach the state fill when Nevada has no loaded counties (expected). Playwright: 6 new tests in region-selection.spec.ts. Nevada: RegionPanel "Nevada", Δlng=2.28; Arizona: RegionPanel "Arizona", Δlng=7.37; Oregon: skipped (above viewport at CA zoom); Sacramento County: RegionPanel "Sacramento County", Δlng=1.94; Merced County: RegionPanel "Merced County", Δlat=0.01 (county is close to CA center horizontally, moved ~25px); LA County: RegionPanel "Los Angeles County", Δlat=3.41. 33/33 pass single-worker (1 skipped). |
| National search + dynamic boundaries + RegionPanel (4 stages) | claude-sonnet-4-6 | 2026-07-31T05:30:00Z | 2026-07-31T08:30:00Z | ~3h | PASS — 3 commits (66d9ebc, 896ffab, 652d3b0). Stage 1: GET /v1/search?q= endpoint (apps/api/src/hfa_api/routes/search.py) — queries raw_us_states/counties/zctas; state by abbr exact-match or name substring (≥3), county by name substring (≥3), ZIP by numeric prefix; returns up to 10 results with type/identifier/display_name/abbr/state_fp/bbox/lon/lat. SearchBar.tsx rebuilt: debounced 300ms AbortController fetch, type badge dropdown (State/County/ZIP), onMouseDown to avoid blur race, Enter/Escape key bindings. flyToRegion() added to MapViewHandle: fitBounds for state/county results (bbox), flyTo for ZIPs. Stage 2+3: Dynamic boundary loading — selectedStateGeoid ("06") and selectedCountyGeoid ("06019") drive separate useEffects that refetch county and ZIP boundaries on change; fetchCountyBoundaries/fetchZipBoundaries updated to accept geoid params. ensureVisible() generalized to use selectedStateGeoidRef/selectedCountyGeoidRef instead of hardcoded Fresno — state tier finds CA feature by STUSPS=CA, county tier finds feature by GEOID=selectedCountyGeoid, ZIP tier computes centroid from county geometry. Verified with LA County search — 300+ LA ZIPs load dynamically. Stage 4: RegionPanel (apps/web/components/RegionPanel.tsx) — summary card shown on state/county map clicks instead of forcing tier switch. Shows AQI for CA state or Fresno County; honest "No sensor data yet" empty state for all others. Tooltip text updated to "Click for summary →". handleStateSelect/handleCountySelect replace handleStateClick/handleCountyClick. 3 Playwright county-tier tests updated for new behavior (RegionPanel visible, tier stays state/county after click). data_contract.md updated with /v1/states/boundaries and /v1/search?q= specs. |
| Interactive historical charts + Pacific time axes | claude-sonnet-4-6 | 2026-07-31T04:00:00Z | 2026-07-31T05:00:00Z | ~60m | PASS — commit 05721a5. Renamed "Last Day" → "Last 24 Hours" throughout (tile label, footer copy). Rewrote MiniChart in DetailPanel.tsx: Y-axis now starts at 0 (was dynamic min); aqiMax = max data value rounded up to nearest 10, minimum 30; Y grid lines at 0 + EPA breakpoints (50/100/150/200/300) within range. X-axis labels now inside SVG (removed outer div): hourly view shows labels at 4-hour UTC boundary ticks formatted as Pacific time (PST/PDT via Intl.DateTimeFormat with timeZone America/Los_Angeles); daily view shows one label per data point as short Pacific date. Interactive hover: transparent SVG rect overlay captures onMouseMove; nearest-point snap via xOf() distance comparison; crosshair line + colored dot + tooltip box rendered as pointer-events:none g element; tooltip shows AQI (fontSize 14, bold) + time/date (fontSize 6.5, gray). Tooltip flips left/right based on right-edge proximity. Gap detection (Oct 27–Nov 17 break) preserved via segment splitting. No new dependencies — pure hand-rolled SVG. Pacific formatters: fmtHourPacific, fmtDateLabel, fmtHourTooltip, fmtDateTooltip all use Intl.DateTimeFormat with UTC timestamps. history-section.spec.ts updated for renamed tile; 4/4 pass in 17.4s, 24/28 total pass (2 flaky pre-existing). |
| Zoom-to-fit per tier using fitBounds on real geometry | claude-sonnet-4-6 | 2026-07-31T05:00:00Z | 2026-07-31T05:30:00Z | ~30m | PASS — commit b9d5108. Added geomBbox() helper in MapView.tsx: computes [[minLng,minLat],[maxLng,maxLat]] from Polygon or MultiPolygon (handles both via forEach). Rewrote ensureVisible() in useImperativeHandle: State tier → fitBounds to CA from stateBoundariesRef.current feature where STUSPS=CA, fallback [[-124.5,32.5],[-114.1,42.1]], padding 20px, maxZoom 7, duration 600ms; County tier → fitBounds to Fresno County from countyBoundariesRef.current feature where GEOID=06019, padding 60px, maxZoom 11, duration 600ms, fallback to CA bounds if feature not yet loaded; ZIP tier → preserved original conditional flyTo (pans to Fresno only if FRESNO_CENTER is outside 80px viewport margin). ZIP behavior preserved for Playwright test compatibility (tests switch directly to ZIP tier from CA zoom and require map to fly to Fresno). 24/28 pass (2 flaky pre-existing). |
| Tooltip toggle + EPA legend decision doc | claude-sonnet-4-6 | 2026-07-31T17:00:00Z | 2026-07-31T17:20:00Z | ~20m | PASS — commit 227aa32. Part 1: Tooltip on/off toggle added at bottom-left of map (Reventure-style pill with inline toggle switch). When off: hover shows no tooltip, no bold outline (feature state hover not set), no cursor change; clicks still work. Implemented via tooltipEnabled prop + tooltipEnabledRef; guard check added to all 3 mousemove handlers; dedicated useEffect clears all hover states when toggled off. Default: on. Session-persistent. 2 Playwright tests in tooltip-toggle.spec.ts pass. Part 3: 6-category EPA AQI legend documented as settled design decision in CLAUDE.md §8 — "do not switch to continuous gradient" with full reasoning. Verified implementation already correct in aqi.ts + Sidebar.tsx. |
| City/place search (raw_us_places import + search extension) | claude-sonnet-4-6 | 2026-07-31T17:20:00Z | 2026-07-31T18:00:00Z | ~40m | PASS — separate commits. Storage check first: cb_2024_us_place_500k.zip = 21.9 MB. 32,612 US incorporated places + CDPs. Stored WITHOUT polygon geometry — only centroid_lon/lat + precomputed county_geoid from one-time spatial join. raw_us_places ≈ 1.6 MB (vs 213 MB for raw_us_zctas with geometry). 32,611/32,612 matched county (99.997%). Backend: _search_places() queries raw_us_places with name LIKE substring, JOINs raw_us_states for state abbr. Places only for 3+ char non-numeric queries. county_geoid added to all result types. Frontend: SearchResult type adds "place" + county_geoid. Selecting a city: switch to zip tier, load that county's ZIPs, fly to city centroid at zoom 11. SearchBar TYPE_LABEL adds "City" badge. Verified: Chicago → Cook County IL ✓; Atlanta GA ✓; Fresno CA → Fresno County ✓. 3 Playwright tests in city-search.spec.ts all pass (5/5). 8 existing tests (boundary-loading + region-selection) still pass. |
| Census/ACS demographics pipeline + 5-bin color legend | claude-sonnet-4-6 | 2026-08-03T00:00:00Z | 2026-08-03T03:00:00Z | ~3h | PASS — Part A (pipeline): pipelines/ingestion/acs/load_acs_demographics.py pulls 8 ACS 5-Year fields at state/county/ZCTA levels for CA. Two vintages (2024+2023) fetched to compute pop_growth_pct and income_growth_pct; only 2024 stored. Population density computed post-insert via DuckDB spatial JOIN against raw_us_* tables. Bug fixed: initial CA_ZIP_PREFIXES missed prefix 937 (all Fresno ZIPs!); corrected to range(900,962). Final import: 1,861 rows (1 state, 58 counties, 1,802 ZCTAs). Sample: CA pop=39,287,377 income=$99,122 density=239.32/mi²; Fresno County pop=1,016,725 income=$74,201 density=168.81/mi²; 93701 pop=9,808 income=$32,768 density=6,436/mi². Part B (API): 3 endpoints in apps/api/src/hfa_api/routes/demographics.py — GET /v1/demographics/states, /v1/demographics/counties, /v1/demographics/zctas (batch). Part C (UI): DemographicsPanel.tsx with 5-bin Reventure-style legend (deep blue→light blue→gray→light red→deep red); RegionPanel shows demographics below AQI for state/county clicks; Sidebar "Community Context" shows DemographicsPanel compact for selected ZIP; TableViewModal population column wired to ACS data (ZIPs still null from zip_now API, counties from countyDemographics lookup, state from stateDemographic). Variables verified: DP05_0001E (pop), DP03_0062E (income), DP05_0018E (age), DP03_0128PE (poverty all people — not DP03_0119PE families), DP02_0060PE+DP02_0061PE (ed<HS — not DP02_0002PE), DP03_0009PE (unemployment), DP02_0115PE (limited English — not DP02_0113PE Spanish speakers), (DP04_0141E+DP04_0142E)/DP04_0136E×100 (housing burden — not single bracket). API verified: /v1/demographics/zctas?geoids=93701,93702,93705 returns full row with all 14 fields. tsc clean (excluding pre-existing test file errors). data_contract.md updated with raw_acs_demographics schema and 3 new endpoint shapes. |
| Sidebar/legend UI overhaul (5 tasks, Reventure.app parity) | claude-sonnet-4-6 | 2026-08-03T14:00:00Z | 2026-08-03T16:30:00Z | ~2.5h | PASS — 5 commits. (1) Remove ZIP rankings from sidebar — rankings move exclusively to Table View (git commit 3 sessions prior). (2) Sidebar restructure: collapsible "Air Quality" + "Demographic" sections with SVG chevron controls; old flat-list ranking removed. (3) SEARCH DATA POINTS input at top — live-filters all 12 metrics (1 AQI + 11 ACS), selecting triggers map recolor. (4) Info icon cards — each metric row shows circular "i" button; click opens fixed-position card overlay (title, description, source attribution, ×close) using getBoundingClientRect to escape sidebar overflow-y-auto clipping; z-9999 card floats over map. 12 METRIC_INFO entries covering all ACS fields and PM2.5 AQI. (5) 7-bin equal-interval legend as map overlay: demographics.ts updated from 5→7 bins (dark-blue→medium-blue→light-blue→neutral-gray→light-red→medium-red→dark-red, equal 1/7 intervals); Legend section removed from Sidebar; new MapLegend.tsx overlay at bottom-right corner shows AQI 6-category list (AQI mode) or 7-bin gradient + min/max labels + gray=no data (demographic mode). Verified: poverty rate legend shows 1.6%–100.0% with correct 7-segment gradient visible in browser. tsc clean (pre-existing test file errors only). |
| Legend range tier-dependent fix | claude-sonnet-4-6 | 2026-08-03T17:00:00Z | 2026-08-03T17:30:00Z | ~30m | PASS — commit 0017964. Root cause: MapLegend always used zctaDemographics (Fresno County ZIPs) regardless of active tier, so county-tier population showed 31→86,110 (Fresno ZIP range) instead of 1,616→9,808,667 (full CA county range). Fix: MapLegend now accepts tier + all 3 datasets (stateDemographics, countyDemographics, zctaDemographics) and selects the correct array per tier. Map coloring in MapView.tsx was already correct; only the legend overlay was wrong. Evidence from raw_acs_demographics: county Population min=Alpine County (1,616), max=LA County (9,808,667); Fresno ZIPs min=93634 (31), max=93722 (86,110); Sacramento ZIPs min=95680 (11), max=95823 (83,607). Playwright: 3 new tests in legend-tier-range.spec.ts assert exact min/max text at county tier, ZIP+Fresno tier, and ZIP+Sacramento tier (proving recomputation on county change). All 3 pass in 39.6s. data-testid="legend-min/max" added to range spans. |
| Table View rebuild — tier-aware, metric-aware, viewport-dynamic | claude-sonnet-4-6 | 2026-08-03T23:00:00Z | 2026-08-04T01:30:00Z | ~2.5h | PASS — Complete rewrite of TableViewModal.tsx + backend support. Backend: (A) GET /v1/zips/cities?county={geoid} — maps each ZCTA in a county to its nearest incorporated city (lsad='25' from raw_us_places, Euclidean distance on pre-computed centroids, lru_cache). Verified: 93701-93726 → "Fresno", 93727/93730/93737 → "Clovis". (B) GET /v1/counties/boundaries now includes CENTROID_LON/CENTROID_LAT in each feature's properties — used for client-side viewport filtering. Frontend (MapView.tsx): MapBounds interface exported; getBounds() added to MapViewHandle; onBoundsChange prop fires on map load + every moveend via useRef to avoid stale closure. Frontend (page.tsx): mapBounds and zipCities state; zip cities useEffect; onBoundsChange={setMapBounds} on MapView. Frontend (TableViewModal.tsx): full rewrite — tier-aware column sets (State: RK|State|[metric]|[secondary]|Pop; County: RK|County|[metric]|[secondary]|Pop; ZIP: RK|Zip|City|[metric]|[secondary]|Pop); selected metric always first data col; secondary logic (median_hh_income → poverty_rate_pct; income/population → median_hh_income; AQI → no secondary); trailing Population omitted when selected or secondary; sortable column headers with ↕/↓/↑ arrows, defaults desc by selected metric, resets on tier/metric change; viewport filter at county tier (county centroid in mapBounds); "Showing X of Y counties (map viewport)" vs "Showing N Counties/ZIPs"; Escape + click-outside close; data-testid="table-view" + data-testid="table-subtitle". Visual verification: county+AQI (58 rows, Fresno ranked 1st AQI=136 USG), county+Median HH Income (9 counties visible at viewport, viewport filter active), ZIP+AQI (55 ZIPs, cities populated: Clovis/Fresno/Coalinga/Huron/Selma), ZIP+Median HH Income sorted by Poverty Rate (100%→46.4%). Playwright: 8 new tests in table-view.spec.ts — all 8 pass in 36.5s. data_contract.md updated with /v1/zips/cities endpoint + CENTROID_LON/LAT on county boundaries. |
